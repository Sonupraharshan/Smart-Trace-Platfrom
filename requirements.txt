# ============================================
# Smart Quality Inspection Platform
# Requirements — Vercel / Production (Lightweight)
# ============================================
# For local development with PyTorch training,
# use: pip install -r requirements-full.txt

# --- ML Inference (ONNX Runtime — ~50 MB vs ~2.5 GB for PyTorch) ---
onnxruntime>=1.17.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
faiss-cpu>=1.7.4

# --- Web Framework ---
Django>=6.0,<7.0
gunicorn>=22.0.0
whitenoise>=6.6.0
dj-database-url>=2.1.0
psycopg[binary]>=3.1.0

# --- Utilities ---
tqdm>=4.65.0
python-dotenv>=1.0.0
